apiVersion: v1
kind: Service
metadata:
  name: svc-{{ include "ala-collectory.fullname" . }}
  labels:
    {{- include "ala-collectory.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "ala-collectory.selectorLabels" . | nindent 4 }}
