apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ala-collectory.fullname" . }}
  labels:
    {{- include "ala-collectory.labels" . | nindent 4 }}
data:
{{ (tpl (.Files.Glob "config/collectory-config.properties").AsConfig . ) | indent 2 }}
